조건문 (if)

const a = 2;
if (a>1){
    a가 1보다 크다
 }else if(a === 1){
    a와 1은 같다
 }else{
    a가 1보다 작다
}

조건식이 일치할 경우 if문의 로직이 실행된다.
만약 일치하지 않을 경우 else에 있는 로직이 실행된다.
그리고 추가적인 조건을 넣고 싶을 경우에는 else if()의 형식을 사용할 수 있다.