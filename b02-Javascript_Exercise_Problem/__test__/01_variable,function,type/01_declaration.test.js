const declaration = require("../../01_variable,function,type/01_declaration")

describe("declaration", ()=>{

    test("variable 'dec' must be declared", () => {
        expect(declaration()).toEqual(undefined);
      });



})