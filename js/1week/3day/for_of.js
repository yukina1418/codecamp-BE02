// 숫자로만 이루어진 배열이 있습니다.
// for-of를 이용해 숫자의 총합을 구해주세요.

const arr = [11, 22, 33, 44, 55];
let sum = 0;

for (number of arr) {
  sum = sum + number;
}

console.log(sum); // 165

// let a = 0;

// while(a < arr.length){
//   sum += arr[a]
//   a += 1
// }

// 숫자로만 이루어진 배열이 있습니다.
// for-of를 이용해 숫자의 총합을 구해주세요.

// const arr = [11, 22, 33, 44, 55];
// let sum = 0;

// for (number of arr) {
//   sum += number;
// }

// console.log(sum); // 165

/*  for of 를 사용하는 방법 for of에서는 객체를 사용할 수 없다.
const obj = {
  title: "The Title",
  name: "Jane",
  contents: "Nothing to say",
};

console.log(Object.keys(obj))

for (let key of Object.keys(obj)) {
  if (key === "title" || key === "name") {
     obj[key] = obj[key].toUpperCase();
  }
}

console.log(obj);

*/
